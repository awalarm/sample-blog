<h1>Контакты</h1>

<%= form_for :contact, url: contacts_path do |f| %>
	<p>
		<%= f.label :email %><br>
		<%= f.text_field :email %>
	</p>

	<p>
		<%= f.label 'Сообщение'%><br>
		<%= f.text_area :message %>
	</p>

	<p>
		<%= f.submit 'Отправить сообщение'%>
	</p>
<% end %>
